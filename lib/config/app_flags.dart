class AppFlags {
  // defaults locales por si no carga app_config/current
  static const bool useRemoteContent = false;
  static const bool enableFreeMode = false;
  static const bool enableChallenges = false;
  static const bool enableAchievementPopups = true;
}
