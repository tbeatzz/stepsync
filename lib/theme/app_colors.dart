import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0E0E1A);
  static const card       = Color(0xFF1A1B2E);

  static const primary    = Color(0xFF00C2FF); // celeste
  static const secondary  = Color(0xFFFF3B81); // magenta

  static const text       = Colors.white;
  static const textMuted  = Color(0xFFB3B3B3);

  static const gradientStart = primary;
  static const gradientEnd   = secondary;
}
